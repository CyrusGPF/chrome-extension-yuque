# YuqueOut + Yuque 顺序拖拽

YuqueOut 负责导出稳定身份和原始语雀目录树；配套的 Obsidian 插件负责导入后的实时顺序。文件名保持简洁，不需要为每篇笔记写入单独的 `order` 字段。

## 导出设置

在 **设置 → 导出结构** 中使用以下默认配置：

- **GUID 随机位数**：默认 64 bit，可选择 72 bit。使用纯数字字母 Base62 后缀，文件使用 `f-`，文件夹使用 `d-`；64 bit 为 11 位后缀，72 bit 为 13 位。
- **`_yuque_order.json`**：每个知识库自动生成，用于一次性初始化语雀原始顺序。
- **层级编号前缀**：关闭。文件名保持简洁。
- **写入 order 排序字段**：关闭。该选项仅为旧版排序插件保留。
- **嵌套文档使用文件夹笔记模式**：开启。包含子文档的父文档会导出为 `Parent/Parent.md`。

清单文件使用 `version: 2`，包含 `exportId`、GUID 位数以及按最终文件系统路径组织的目录和直接子项。图片等附件及其父目录单独登记在 `resources` 中，参与完整性校验和身份初始化，但不会插入或打乱语雀正文顺序。插件只保留“恢复原语雀目录顺序”入口。同一次导出选择多个同名知识库时，根目录自动使用 `-1`、`-2` 隔离，文档、附件和清单使用完全一致的最终根目录。导出不再生成 README 索引；非 Markdown 文件也通过清单获得 `f-` 身份并参与排序。

## 安装与使用

在 `YQSorting` 目录执行 `npm install` 和 `npm run build`，然后将 `main.js`、`manifest.json` 和 `styles.css` 复制到：

```text
<仓库>/.obsidian/plugins/yuque-order-drag/
```

在 Obsidian 中启用 **Yuque Order Drag**。插件会扫描 Markdown 文件，为旧笔记补充稳定的 `guid`；同时在插件的 `data.json` 中为普通文件夹创建身份，并等待用户显式执行清单初始化。

1. 在文件列表中拖拽项目，可以调整同级项目的顺序。
2. 将项目拖到文件夹，或拖到一个已经存在对应文件夹的文件夹笔记上，可以将项目移动到该文件夹中。
3. 首次导入后执行 **恢复原语雀目录顺序**。新清单首次采用身份，后续只恢复顺序。

插件支持完整 GUID 查重、64/72 bit 全库换号、最多三个恢复点，以及当前会话 50 步无时限撤销。撤销命令不占用默认快捷键，可在 Obsidian 设置中自行绑定。

顺序存储为 `orderByFolder[folderGuid] = [childGuid, ...]`。重命名或移动 Markdown 文件不会丢失身份，因为 guid 会随 frontmatter 一起保留。删除文件时只会移除它的 guid，不会重新编号相邻项目。新建笔记会按照配置插入到顶部或底部。

## 限制

- 文件列表的拦截依赖 Obsidian 内部的 `Folder.sort` 实现，并且带有保护逻辑。如果未来版本发生变化，插件会回退到默认树顺序，不会修改笔记。
- 通过 Windows 资源管理器修改 Markdown 文件通常是安全的，因为 guid 保存在文件中。普通文件夹的身份保存在 `data.json` 中，因此文件夹的结构性重命名或移动最好在 Obsidian 内完成。
- 顺序保存在插件的 `data.json` 中。跨设备使用时请同步该插件数据；只有用户主动执行恢复命令时，已消费的 `exportId` 才会再次应用。
