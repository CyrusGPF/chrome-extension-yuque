# Obsidian 导出结构与插件配置说明

> 当前 V1 方案以 [OBSIDIAN_ORDER_DRAG_V1.md](./OBSIDIAN_ORDER_DRAG_V1.md) 为准：使用 `guid` + `_yuque_order.json` + Yuque Order Drag 插件。本文下面保留的旧版前缀/order/第三方排序插件说明仅用于历史兼容。

本文档整理 YuqueOut 导出到 Obsidian 的目录展示方案，以及两个配套插件（Folder Notes、Custom File Explorer Sorting）的配置方法与踩坑经验。适用于将语雀知识库批量导出后，导入 Obsidian 长期使用、并保留语雀原始目录顺序与嵌套层级的场景。

---

## 一、导出到 Obsidian 的目录展示方案

修改扩展设置（设置页 → 导出结构）里的几项后，导出的知识库会呈现如下结构：

```
语雀备份/
└── 我的知识库/
    ├── 01-章节/                  // 顶层文档「章节」（有子文档 → 文件夹笔记）
    │   ├── 01-章节目录.md         // 父文档：与文件夹同名（folder note）
    │   ├── 01-子文档.md           // 子文档
    │   └── 02-子文档2.md
    ├── 02-文档.md                 // 普通顶层文档
    ├── attachment/                // 图片等附件按知识库集中（可自定义名字）
    └── README.md                  // 按语雀顺序生成的索引
```

导入 Obsidian 后：文件树按名称即保持语雀目录顺序（01 在 02 前、嵌套层级正确）；点击文件夹打开父文档；附件独立一个文件夹，删库连带删除。

### 1. 层级序号前缀
按语雀目录顺序为文件名/文件夹加 `01-`、`02-` 前缀，Obsidian 默认按名称排序即可还原语雀顺序。
**设置**：导出结构 → 层级序号前缀（默认开）。

### 2. Obsidian 文件夹笔记模式（嵌套文档）
父级文档存为 `父目录/父目录.md` 的"文件夹笔记"结构，子文档平铺其下。配合 Folder Notes 插件，点击文件夹名即可打开父文档。
**设置**：导出结构 → 嵌套文档文件夹笔记模式（默认开，仅影响 Markdown）。

### 3. README 索引
每个知识库根目录生成 `README.md`，按语雀原始顺序缩进列出全部文档链接，作为知识库的导航首页。若图片下载失败，会在末尾生成"未下载成功的图片"小节（含原始链接），方便手动补充。
**设置**：导出结构 → 生成 README 索引（默认开）。

### 4. frontmatter 写入 order 字段
每篇 md 文件头部写入 `order: 1.1`（层级序号），例如：

```markdown
---
order: 1.1
---

# 章节内容
```

这个字段是给排序插件准备的"备用顺序数据"。以后若想去掉文件名里的数字前缀（让标题更干净），只需删除前缀、再用排序插件按 `order` 字段排序即可，顺序不丢。
**设置**：导出结构 → 写入 order 排序字段（独立开关，默认开，与「层级序号前缀」互不依赖，可单独关闭）。

### 5. 附件按知识库集中
图片等附件保存到知识库根目录的 `attachment/` 文件夹（不再是每篇文档一个 assets）。多个知识库共用一个 Obsidian 库时互不干扰，删除某个知识库时其附件一并删除。
**设置**：
- 图片附件目录：`每个知识库一个 attachment（推荐）` / `每篇文档一个 assets`
- 附件文件夹名称：默认 `attachment`（可改成 `images`、`.attachments` 等）

### 6. 图片命名防冲突
集中到 attachment 后，图片文件名带文档 id（例如 `文档名-101-1.png`），避免同名文档的图片互相覆盖。

### 7. 同名知识库确认
每次点击导出前，弹窗提醒检查目标下载路径下是否已有同名知识库目录：
- 继续导出：同名文件直接覆盖（不留 `(1)` 副本）
- 清理旧导出数据：打开下载文件夹，方便手动清理旧目录后重导
- 不再提示：勾选后记住，以后不再弹（可在设置里重新开启）
**设置**：导出结构 → 导出前确认重名目录（默认开）。

### 8. 重导同名文件冲突处理
同名文件默认直接覆盖，不留副本；旧行为是自动加 `(1)`。
**设置**：导出结构 → 同名文件冲突（`覆盖（推荐）` / `保留副本`）。

---

## 二、Folder Notes 插件使用方法

**作者**：Lost Paul（插件市场里搜 "Folder Notes"，注意别装成同类其他插件）

### 安装
Obsidian → 设置 → 第三方插件 → 关闭安全模式 → 浏览 → 搜索 `Folder Notes` → 安装并启用。

### 核心功能
- 点击文件树里的**文件夹名**直接打开该文件夹的同名笔记（正是我们的 `父目录/父目录.md` 结构）
- 可设置从文件树**隐藏同名笔记文件**（这样文件树里只剩文件夹，点击即进内容）
- 文件夹名样式化、自动同步文件名等

### 关键设置
设置 → Folder notes → **File explorer** 标签：
- **Hide folder note**：隐藏文件夹笔记文件（推荐开启，文件树更干净）
- 其余保持默认即可

### 重要坑
修改设置后，文件树可能不即时生效——**必须 `Ctrl + R` 重载 Obsidian**（或完全重启）才会隐藏同名笔记。这是 Obsidian 文件树缓存问题，不是插件或导出问题。

---

## 三、Custom File Explorer Sorting 插件使用方法

**作者**：SebastianMC（插件市场搜 "Custom File Explorer Sorting"，插件 ID `custom-sort`）。它是 Obsidian 排序插件里用户量最大的（约 20 万下载）。

### 安装
Obsidian → 设置 → 第三方插件 → 浏览 → 搜索 `Custom File Explorer Sorting` → 安装并启用。

### 配置（sortspec.md）
在 Obsidian vault 里新建一个文件 `sortspec.md`（名字必须精确），填入：

```yaml
---
sorting-spec: |
    target-folder: /
    < a-z
    target-folder: /*
    < a-z
---
```

- `target-folder: /`：应用到 vault **根**文件夹
- `target-folder: /*`：应用到根下的**所有子文件夹**（递归）
- `< a-z`：文件夹和文件**混排**，按名称自然升序（数字前缀 `01`/`02` 会正确排序）

### 应用步骤
1. 在 vault 里创建 `sortspec.md`，粘贴上面的内容
2. 点击 Obsidian 左侧的**丝带按钮**（插件图标），让插件读取并应用
3. 若没立即生效，`Ctrl + R` 重载 Obsidian

### 两个关键坑
1. **target-folder 路径要对准 vault 根**：`/` 指的是 `.obsidian` 所在的**那层**（即 vault 根）。如果知识库内容就在 vault 根下，就用 `/` 和 `/*`；不要写成 `/语雀备份/知识库` 之类的子路径（除非知识库确实在子目录里）。可用文件资源管理器看 `.obsidian` 在哪一层，这一层就是 `/`。
2. **用源码模式编辑 sortspec.md**：Obsidian 属性面板粘贴多行 YAML 容易破坏缩进（YAML 缩进敏感，`target-folder` 和 `< a-z` 前是 4 个空格）。请用它对应的**源码模式**或记事本/VSCode 编辑，确保内容如下（缩进为 4 空格）：

```
---
sorting-spec: |
    target-folder: /
    < a-z
    target-folder: /*
    < a-z
---
```

### 它解决了什么
Obsidian 默认"文件夹优先排序"，导致 `父目录.md`（folder note 文件）被排到文件夹内容末尾。用 `< a-z` 混排后，`父目录.md` 会排到 `父目录-1` 之前，符合语雀目录顺序的视觉直觉。若未来去掉文件名数字前缀，还能改配为按 frontmatter `order` 字段排序。

---

## 四、使用流程速查

1. 在扩展设置页确认"导出结构"各项（默认已开启，通常无需改动）
2. 导出知识库 → 文件保存到 `语雀备份/知识库名/`
3. 把该文件夹放入（或复制进）Obsidian vault
4. 安装并配置 Folder Notes（隐藏同名笔记）+ Custom File Explorer Sorting（sortspec.md）
5. `Ctrl + R` 重载 Obsidian 让设置生效
6. 打开 `README.md` 作导航，按目录顺序浏览

做完这些，Obsidian 里的文件树顺序与语雀一致，点击文件夹直接打开父文档，附件按库独立，重新导出不留副本。
---
## 五、顺序怎么维护：测试期 vs 切到 Obsidian 之后

### 先判断你在哪个阶段
- **测试期（反复重导）**：语雀仍是源头，会反复增删/重排文档。此时以「导出工具写进文件的前缀和 `order` 字段」为顺序真值，**不要**把 Flexplorer 的 Custom order 当 master——每次重导后文件路径变了，插件记住的顺序会失配，需要重新 `Save as custom order`。
- **切换后（不再用语雀，Obsidian 唯一源头）**：定稿导出一次，之后所有笔记在 Obsidian 内部创建。此时**只留一种顺序来源**，并长期沿用。

### 切换后：二选一，只留一个顺序真值
| 方案 | 你要做的 | 顺序存哪 | 新笔记如何归位 |
|------|---------|---------|---------------|
| A. 按 `order` 字段排序（延续现有字段，推荐） | 新建时给一个 `order` 值（可做成模板） | 文件 frontmatter，可跨设备/换库 | 填了 `order` 就自动插到同类位置 |
| B. Flexplorer Custom order | 直接拖拽 | 插件 `data.json` | 拖一次归位 |

- A 依赖能按 `order` 字段排序的插件（如 Custom File Explorer Sorting 的配置驱动排序）；B 用 Flexplorer。
- 共同原则：**别同时维护两套**，那才是真正的负担。

### 关于文件名数字前缀
前缀的作用是在「Obsidian 默认按名称排序」时还原语雀顺序。切到 Obsidian 后若想让标题干净，可去掉前缀、改用上面的顺序方案——前缀可删，`order` 仍在文件里，顺序不丢。
注意：「层级序号前缀」与「order 字段」现在是**两个独立开关**（默认均开）。想「只写 order、不加文件名前缀」只需关掉前者；想「只加前缀、不写 order」则关掉后者。
